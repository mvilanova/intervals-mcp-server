# Replace dashboard.yourdomain.com with your real domain before first deploy.
# Caddy provisions a Let's Encrypt cert automatically on first request.

dashboard.yourdomain.com {
    reverse_proxy web:3000
    encode gzip
}
